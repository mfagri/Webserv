/////////////////////////////////
This attribute specifies the content type used to submit the form to the server (when the value of method is "post"). The default value for this attribute is "application/x-www-form-urlencoded". The value "multipart/form-data" should be used in combination with the INPUT element, type="file".
/////////////////
if no content type 

https://docs.imperva.com/bundle/on-premises-knowledgebase-reference-guide/page/post_request_missing_content_type.htm

Invicti detected a missing Content-Type header which means that this website could be at risk of a MIME-sniffing attacks.

if lenght == 0 and !Transfer Encoding => ERROR

parse request line -> if no error -> parse headers -> if no error -> parse body 

parse headers

content type ,content lenght , Transfer Encoding ,host,

if(chuned)
{
    body => chunked;
    (Content_Type)
    {
        if(multipart/form-data; boundary=--------------------------381898572940245678379138)
            search(Content-Disposition: form-data; name="mfagri")
        if(application/x-www-form-urlencoded)
            search(n=v&nn=vv)
    }
}
//////////////
9
n=v&nn=vv
0
|
//////////////